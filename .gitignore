__pycache__/
*.py[cod]
*$py.class

.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual envs
.venv/
venv/
env/

# Workbooks — personal data (portfolio + cosmetic edits live only on your machine)
*.xlsx
*.xlsm
~$*.xlsm
stocks_report_*.xlsx

# Run logs
*.log

# Speedup F: yfinance .info SQLite cache (lives next to the workbook)
stocks_info_cache.sqlite
stocks_info_cache.sqlite-journal

# Binary deliverable snapshots
*.zip

# OS / editor
.DS_Store
.vscode/
.idea/
.claude/  # all Claude Code local session state (locks, worktrees, settings)

# Personal scratch / notes — not for the public repo
NEW_FEATURES.md
