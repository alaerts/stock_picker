When you pick this back up: read CLAUDE.md → MEMORY.md → HANDOFF.md in that order, then run get-quotes once (it'll self-heal the AutoFilter + restyle any unstyled cells from the latest fixes).
The status "Writing quote columns for 986 rows…" is displayed for a long time, too long. 
The format of the currencies in the sheet Currencies and Main should be comma style (with two decimals)
In the Help sheet, add a row per major version, with a short summary of the changes
Earlier, I asked you to implement the "In-Excel cell-based stop". I cannot find it.

